/**
 * Conformité légale pour les webhooks d’ingestion (marchand doit avoir accepté les CGU).
 * Texte utilisateur : `Api.errors.webhookLegalCompliance` dans `messages/{locale}.json`
 * (voir routes `app/api/webhooks/zenith` et `app/api/webhooks/[api_key]`).
 */
