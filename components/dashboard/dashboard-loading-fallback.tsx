'use client';

/**
 * Chargement dashboard : pas d’écran plein logo (le shell reste visible).
 * @deprecated Conservé pour imports éventuels ; préférer `fallback={null}` sur Suspense.
 */
export function DashboardLoadingFallback() {
  return null;
}
